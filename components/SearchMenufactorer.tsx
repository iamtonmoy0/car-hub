import { SearchMenufactorer } from "@/types";
import React from "react";

export default function SearchMenufactorer({
  menufactorer,
  setMenufactorer,
}: SearchMenufactorer) {
  return <div>SearchMenufactorer</div>;
}
